ANTHROPIC_API_KEY=sk-ant-...
WA_BRIDGE_URL=http://localhost:3001
SCHEDULER_TIMEZONE=Asia/Kolkata
SCHEDULER_HOUR=8
SCHEDULER_MINUTE=0
DB_URL=sqlite:///./wishing_bot.db

# AI provider: "auto" (try local first, fall back to Claude), "claude", "local"
AI_PROVIDER=auto
# Local AI base URL (LM Studio default: http://localhost:1234/v1, Ollama: http://localhost:11434/v1)
LOCAL_AI_URL=http://localhost:1234/v1
# Specific local model to use (leave blank to auto-detect first available)
LOCAL_AI_MODEL=
